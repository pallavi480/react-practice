import React from "react";

const Hooks = () => {
    return (
        <div>
        <h1>
            Hooks are special function provided by react allow us to handle life cycle method and state
        </h1>

        <h2>
        <li>Use Hooks only inside function components</li>
        <li>Use Hooks the top level </li>
        <li>Hooks name must start with "use"</li>
        </h2>

        <p> UseState hook - Manage Data - state </p>
        <p>useEffect hook - Side Effect </p>
        </div>
    )
}

export default Hooks